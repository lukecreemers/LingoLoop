{
  "name": "lingoloop",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "backend:dev": "npm run start:dev -w backend",
    "backend:build": "npm run build -w backend",
    "shared:build": "npm run build -w @lingoloop/shared"
  }
}

